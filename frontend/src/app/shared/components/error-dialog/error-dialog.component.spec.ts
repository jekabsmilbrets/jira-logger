import { ErrorDialogComponent } from './error-dialog.component';

describe('Shared Components error-dialog.component', () => {
  it('class is defined', () => {
    expect(ErrorDialogComponent).toBeTruthy();
  });
});
