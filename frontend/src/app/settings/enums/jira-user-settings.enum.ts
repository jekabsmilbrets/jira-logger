export enum JiraUserSettings {
  userTimeZone = 'jira.user-time-zone',
  locale = 'jira.locale',
}

export enum JiraUserSettingSlugs {
  userTimeZone = 'jira-user-time-zone',
  locale = 'jira-locale',
}
