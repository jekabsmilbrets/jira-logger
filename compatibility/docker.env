# Disposable acceptance project only; no deployment credentials.
APP_ENV=prod
APP_SECRET=disposable-compatibility
APP_INTERNAL_TIMEZONE=UTC
APP_DEFAULT_USER_TIMEZONE=Europe/Riga
POSTGRES_VERSION=13
POSTGRES_DB=compatibility
POSTGRES_USER=compatibility
POSTGRES_PASSWORD=disposable
DATABASE_URL=postgresql://compatibility:disposable@db/compatibility?serverVersion=13&charset=utf8
CORS_ALLOW_ORIGIN=^https?://localhost$
API_HOST=http://127.0.0.1:18084
API_BASE=/api
